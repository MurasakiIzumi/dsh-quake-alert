# Changelog

本文件记录 dsh-quake-alert 的显著变更，格式参照 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)。

## [0.3.2] - 2026-09-11

### Fixed

- **河川予報区域表从未下发（0.3.0 起的功能断点）**：Host 的 `/areas` 只返回市区町村表，而 `lib/data/river-areas.js`（338 区域 / 884 市町村）虽然由 `pnpm build:areas` 生成，却没有任何代码引用它。于是 Client 侧 `riverAreaCities()` 恒为空，指定河川洪水予報（VXKO）的区域全部退化为 `pref: ''` + `prefUnknown`，而匹配层对「归不到市町村」的条目一律放行——**关注任何地区的用户都会收到与自己无关的县的水害警报，市级收窄也完全失效**。现在 `/areas` 在同一份响应里下发 `riverAreas`
  - `/areas` 的 `cache-control` 从 `public, max-age=86400` 改为 `no-cache`：两张表都会随插件升级变化，长缓存会让修复 / 更新延迟一天才生效
- **河川区域表与市区町村表的名称写法不一致（假名）**：気象庁的不同数据源对同一市町村写法不同——総務省コード表「金け崎町 / 六ゖ所村 / 南あるぷす市」vs 河川区域 CSV「金ケ崎町 / 六ヶ所村 / 南アルプス市」（8 例）。`prefsOfCity()` 的索引键与查询键都不做假名归一，这 8 个市町村因此反查不到归属县（放行 → 误报）；而且即使反查成功，河川表的写法被直接写进 `region.city` 后，与用户勾选的**本表**名字（如「金け崎町」）比对也会失配（**漏报**）
  - `normKana()` 改为两级归一：① 平假名 → 片假名（け→ケ、ゖ→ヶ）② ケ → ヶ。两步都要——只做第一步会让「ケ」与「ヶ」不再相等，反而破坏既有的ケ/ヶ 等价
  - 新增 `canonicalCityOf()`：把外部写法换成市区町村表的规范名后再放进 `region.city`；索引、反查、比对全部经 `normKana`
  - `regionInWeatherWatch()` 的市町村比对改为归一等价比较（`cities.some((c) => normKana(c) === normKana(region.city))`）
- **刷新页面重放历史警报**：Client 的 feed 游标是闭包变量、每次页面加载都从 0 开始，于是刷新 / 新开标签页会把 Host 环缓冲里（最多 120 条、含完整电文原文）的电文当新消息重新走一遍 `handleAlert`——其中 L4 以上的会**再次响铃 + 弹窗**（跨标签页去重只有 5 秒窗口，拦不住后加载的页面）
  - 游标落盘到 `dsh.quakeAlert.feedCursor`，刷新后从上次位置继续；脏数据（非数字 / NaN / 负数）一律当作「没有记录」
  - 首次启动（本地还没有游标）走新增的路由语义 `?since=tail`：只对齐当前位置、不回历史，避免第一次打开就把几小时前的旧警报当新闻
  - 若对面是**不认 `since=tail` 的旧版 Host**（响应里没有 `tail` 标记），同样只取游标对齐、不把那些历史当增量——「只刷新页面、不重启 Host」的升级路径也不会重放
- **Host 重启后静默失联**：Host 的游标原本从 0 重新计数，重启后 Client 手里那个更大的游标会让 `/feed` 的 `entries` 恒空——连 `truncated` 都不为真——而状态点仍是绿色「已连接」，用户看不出任何异常
  - Host 检出 `since > cursor` 时按 0 补齐缓冲并置 `reset`；Client 见到 `reset` **或**游标倒退都对齐到 Host 的当前值（不只看标记：Host 漏标记时也必须自愈）
  - 更根本的一步是让游标**跨进程单调**：游标起点从 0 改为时间戳（`cursor = now()`），重启后新游标天然大于上一个进程给出的任何游标，旧游标继续可用——既不撞车，也不会出现「旧游标 ≤ 新游标但缓冲前段没拿过」的漏报。`truncated` 的判据随之补上「确实发生过淘汰」（`dropped > 0`）：seq 不再从 1 连续编号，旧判据会把 `from=0` 误报成有缺口
  - 时钟回拨（NTP 校正 / 改系统时间）会让游标倒退，由 `reset` 兜住
- **冷启动把「刚发布的电文」也当历史丢掉（静默漏报）**：真正的冷启动发生在 Host 启动之后约 1 分钟（首轮 `pollOnce` 先被按需轮询 skip 掉，要等 Client 首次读 `/feed` 才 `markRead`），而旧判据把那一轮看到的一切都算历史——那一分钟里发布的真实警报被永久记为已见、永不处理。现在冷启动的基准改成**进程启动时刻**（`isFreshEnough` + 新增 `startedAt` 注入点）：启动之前的历史仍然只记已见，启动之后发布的照常拉详情、产事件。另设 2 分钟时钟容差（`startToleranceMs`，可置 0 回到严格模式），覆盖「电文在 Host 启动前一分钟发布、第一次轮询还没轮到它」的边界——重启时最多补播最近两分钟的电文（通常 0～1 条），不会放大成刷屏
- **外部请求没有超时**：`lib/poller.js` 的默认 `fetchText` 与 Client 的 `defaultFetchJson` 都没有 `AbortSignal`——对端半挂起时 `inFlight` 一直占着、`schedule` 的 `await` 不返回，整条轮询静默停摆，连 `errors` 计数都不增加（看起来像「没有新消息」）。现在 Host 侧 20 秒、Client 侧 10 秒超时（默认抓取实现抽成可单测的 `createFetchText`）
- **响应体没有大小上限**：Host 现在对 feed / 详情响应设 2MB 上限，异常大的响应让这一轮失败，而不是把内存吃满
- **WebSocket 半开连接静默失效**：NAT / 代理静默断开时 TCP 已不通，但浏览器**不会**触发 `onclose`，状态点一直显示绿色「已连接」却收不到任何推送——对预警产品这是最危险的失效模式（用户以为自己在被保护）。现在记录 `lastActivityAt`，超过 20 分钟毫无活动（P2PQuake 约 10 分钟就会强制断线重连，正常不会走到这里）主动断开重连，状态文案写明「久无数据」
- **气象条目的来源标错**：历史记录展开详情时，气象电文落进最后的 `else` 分支被标成「code 551」（地震速报代码）。现在气象显示「JMA 电文」（新增可单测的 `p2pCodeTextOf`）
- **震度4 命中显示成「信息蓝」**：`severityOfScale` 会返回 `'yellow'`，但 `sevColor` 只认 red / orange，其余一律返回蓝色——默认阈值 40 下最常见的命中档配色与语义不符。现在 yellow → `#d9a406`
- **气象条目在历史列表里落灰色兜底**：`kindColor` 缺 `weather` 键。现在补上紫色（`kindColorOf`，索引经 `own()`）
- **跨标签页配置同步只在设置页打开时有效**：`storage` 监听原先写在设置页组件里，没打开设置页的标签页会一直按旧配置提醒（还继续拉 feed、维持 Host 轮询）。现在移到 `apply` 的常驻 effect
- **音频节点不回收**：`osc.stop()` 只停止发声，节点仍挂在 `destination` 上；每次警报新建 2～3 个，长期运行持续累积。现在播完统一 `disconnect()`
- 零碎修正：`s.checkbox` 未使用的 `color` 形参；quake 命中且 `kindLabel` 不含「各地」时通知标题尾部悬空的「 · 」；`loadCityTable()` 未纳入 effect（停用后仍会写 store / 配置，现在用 `AbortController` 中止并回到 `idle`，下次装载可重试）
- 安全加固——这条路由没有来源校验（dsh 的 webServer 本身也不校验，`host` 可配 `0.0.0.0`），所以把"一次能拿走多少"卡死：`since` 缺失或非法（`abc` / `1e999` / 负数 / **空串与纯空白**——`Number('')` 是 0，会被当成「从 0 取」）**不再按 0 处理**（那等于让任何请求者一次把整个环缓冲拿走），改为一律按 `tail` 对齐；`/feed` 单次最多返回 `MAX_FEED_ENTRIES`（50）条并标记 `more`，Client 见到 `more` 改用**最后一条实际返回的 seq** 推进游标——截断因此不会漏报（独立复验：60 条缓冲经 50 + 10 两轮拿全，游标追平 Host）

### Changed

- 回归断言从 346 项扩展到 433 项。P1/P4 部分关键在于补上了**此前缺失的那一环**：旧断言直接 `import` 数据表后调用 `setRiverAreas()`，绕过了「Host 是否真的把表发下来」这个唯一的断点，所以那两个缺陷逃过了全部 346 项断言。新增：
  - `/areas` 响应含 `riverAreas`、Client **仅凭该响应**完成装配（`loadCityTable()` → `cityTableState === 'ready'`）
  - 河川表 884 个市町村在本表中的名称一致性（0 例未收录）
  - 假名归一与规范名反查（金け崎町 / 金ケ崎町、南あるぷす市 / 南アルプス市）
  - 真实 12 位区域码 + L4「氾濫危険情報」电文的端到端匹配：无关县不提醒、对应县提醒、市级收窄命中与未命中、跨表写法的市级命中
  - 游标生命周期：首次 `tail` 对齐、持久化后刷新不重放、Host 重启（`reset` 或游标倒退）自愈、真实 localStorage 读写、脏游标当作无记录
  - Host 侧 `snapshot` 的 tail / reset / truncated 语义，含「真淘汰才报 truncated」与「游标以时间戳为起点」
  - 冷启动窗口（启动之后发布的必处理、启动之前的仍跳过）、外部抓取的超时信号与体积上限、非法 `since` → `tail`、`capFeedEntries` 截断与 `more`、Client 见到 `more` 时按末条 `seq` 推进并最终追平
  - WebSocket 半开（久无数据 → 主动重连；持续有消息 → 不误判）、音频节点播完 `disconnect`、跨标签页常驻监听、城市表请求可中止
  - UI 映射：气象条目的来源标注、severity 配色（yellow）、灾种配色（weather）、通知标题分隔符
  - 上传前的回归审查又补三项：空 / 纯空白 `since` → `tail`、冷启动的 2 分钟时钟容差（容差外仍跳过、可置 0）、旧版 Host（不认 `tail`）不重放历史
  - 顺带解耦：feed 用例之间不再通过沙箱 localStorage 隐式共享游标（P2 加上持久化后暴露出来的测试耦合）

### Notes

- 修复后 `/areas` 单次响应约 67KB（市区町村表 + 河川区域表），仍只在页面加载时拉取一次；`/feed` 正常增量是 0～几条
- 升级需重启 `dsh web`：`/areas`、`/feed` 与轮询器都在 Host 半边；仅刷新页面只会更新 Client bundle
- 旧版（≤0.3.1）从不写游标键，所以升级时不存在需要迁移的历史游标
- 为可测性新增的注入点：`createPoller({ startedAt })`、`createFetchText({ timeoutMs, maxBodyBytes })`、`createWsClient({ staleAfterMs, staleCheckMs })`

## [0.3.1] - 2026-09-11

### Added

- **设置页「发送测试气象警报（轮换场景）」按钮**（「灾害类型」节）：用本地构造的电文走完整链路 —— 解析 → 匹配 → 播报 → 历史，**不发任何网络请求**，可反复点击。每次点击轮换一种场景，覆盖链路上的不同分支：
  - 泥石流警戒情报（市町村级；电文标题本身就是 L4 相当）
  - 指定河川洪水予報 氾濫危険情報（级别写在 Headline 主文里）
  - 大雨危険警報 / 高潮危険警報（级别写在 Kind 名称里）
  - 泥石流警报 L3（边界另一侧：**刻意不播报**，用来演示 L1〜L3 的处理方式）
  - 电文区域取「关注列表的首个都道府县」：写死一个县的话，关注别处的用户点下去会被匹配挡掉、什么都不发生，反而以为插件坏了；没选地区（全日本模式）时退回東京都
  - id 与 EventID 都带时间戳与场景名，避免第二条被消息级去重挡住、或被事件级去重当成「强度未升级的重复发布」而只记历史、不播报
  - 测试提醒**不被静默时段吞掉**：测试的语义是验证提醒链路，被静默悄悄抑制会让人误判成插件故障（`handleAlert` 新增仅供测试使用的 `skipQuietHours`）
- **气象警报独立音色**：此前气象警报落到了地震音分支（`playAlertSound` 只按 eew / tsunami / 其他分流），现改为下行三音 + triangle 波形，与地震（上行双音 sine）、EEW（急促方波）、海啸（低频长音 sawtooth）都区分开。设置页新增「试听气象音」按钮；音色选择抽成可断言的 `soundKindOf()`

### Fixed

- **「静默提示」与事实矛盾**（试用时发现）：`updateWeatherHint` 原先对 L3 及以上都写提示，而设置页与侧边栏的文案固定是「未达 L4，未播报」——于是 L4 已经响铃弹窗之后，旁边仍挂着一行「未达 L4，未播报」。现在该提示的语义收窄为「命中关注地区但**没有播报**」，L4 以上播报后会清除它
- **提示里的地区名重复**：府県予報区级条目的 `area` 与 `pref` 常是同一个名字（東京都 + 東京都），直接拼接会显示成「東京都東京都」。统一改用 `areaLabelOf` 的拼接规则
- **测试提示写死了「应看到提示音与弹窗」**：关闭「气象灾害」开关后点测试按钮，实际不播报、历史也正确记成未触发，但蓝色提示仍说「应看到提示音与弹窗」，会让人误会。现在 `handleAlert` 如实返回 `{ notified, reason, detail }`，提示按**实际结果**生成——开关关闭 / 未达 L4 / 静默时段 / 其它标签页已提醒 / 落在去重窗口内，都会写明真实原因
- **`check-imports.mjs` 的误报**：`stripNoise` 原先用一串 replace 正则做词法剥离，遇到 `decode()` 里 `.replace(/&#39;/g, "'")` 这种「双引号包着单引号」的写法时，单引号正则会一路对到很远的下一处引号，后面整个文件的字符串边界因此错位——XML 字符串里的 `version="1.0"`、`codeType="…"` 被当成「未声明赋值」误报。改为逐字符状态机扫描（注释 / 单双引号 / 模板串各自成段）

### Changed

- 回归断言从 320 项扩展到 346 项：5 个测试场景的解析与级别判定、归县、事件键唯一性、匹配命中与未命中、L3 不播报、静默穿透、静默提示的写入与清除、音色映射、以及 `handleAlert` 的四种未播报回报

## [0.3.0] - 2026-09-11

### Added

- **气象灾害预警（JMA 防災情報XML）**：新增 Host 侧轮询器，取気象庁的高頻度 Atom feed（`extra.xml`，毎分更新）并做两级抓取（feed 列表 → 详情电文），把详情原文交给 Client 解析。设置页新增「气象灾害」开关（默认开）
  - 覆盖电文：`VXWW50` 土砂災害警戒情報、`VXKO` 指定河川洪水予報、`VPWW55/56/57` 気象警報・注意報（大雨 / 土砂 / 高潮）等
  - **播报边界固定在警戒レベル4 以上**：级别是气象厅写在电文里的（`レベル４大雨危険警報` 这类 Kind 名称、「【警戒レベル２相当情報［洪水］】」这类主文），我们**只读不推算**。L1〜L3 仍然拉取、解析并记入历史；L3 命中时只在侧边栏的悬停提示里加一行（不响铃、不弹窗）
  - 不给阈值旋钮：设置页只有「开 / 关」。L1/L2 要求的行动（确认 hazard map）不是桌面弹窗能承载的，L3 面向老年人等需提前避难的人群——README 附有警戒レベル对照表说明每级是什么程度
  - 关注地区复用现有模型（都道府县 + 市区町村），不新增「关注河川」概念：泥石流电文的区域本身就是市町村；洪水电文是河川予報区域，先经新增的河川区域表映射到市町村再比对。**判县优先用区域码前两位**（前两位即都道府県码：`011011`→北海道、`4010000`→福岡県），因为名称有 25 例同名跨县，且已改制的旧名会把历史电文认到别的县（福岡県「那珂川町」会落到栃木県那珂川町）
  - 区域归一不到的条目一律放行（宁可多报绝不漏报）
- **河川予報区域表** `lib/data/river-areas.js`（338 个区域 / 884 个市町村 / 1,567 个区域×市町村对）：由 `scripts/build-areas.mjs` 从気象庁公开的两份 CSV 生成，**以 12 位区域代码为主键**——实测「荒川」对应 5 个不同代码，名称不能当主键
- **零依赖 zip 读取器** `scripts/lib/zip.mjs`：构建脚本直接从官方 zip 取源，用 Node 内置 `zlib`，不引入 unzip / xlsx 依赖（気象庁的代码表是 xlsx，但河川区域 CSV 是纯文本，够用）
- 设置页新增「灾害类型」小节（地震 / 海啸 / 气象灾害三个开关），顺便补上此前**有 schema 字段却没有 UI 开关**的缺口
- 构建脚本 `pnpm build:areas`（联网取源生成区域表；`--check` 只校验，`--from <zip>` 可离线复跑）

### Changed

- **外部请求只由 Host 发起**：Host 是每台机器唯一的外部请求者，多标签页 / 多窗口不会放大请求——気象庁明文要求「一度取得したファイルを再度取得しない」，违反会被封 IP。Client 只从本地回环路由拉增量
- **按需轮询**：10 分钟内没有任何增量读取就跳过这一轮。用户关闭「气象灾害」后 Client 不再拉取，Host 随之自然停下——不需要把 Client 配置读到 Host 侧，也保证关掉开关就不产生任何外部请求
- **冷启动不回放历史**：首次运行把 feed 里的 entry 全部记为已见但不产事件（实测 feed 覆盖可达 10 小时），避免第一次打开就把历史当新闻刷屏
- **详情拉取失败的 entry 记为已见、不再重试**：宁可漏一条，也不对同一个坏 URL 每 60 秒反复请求
- `handleRaw` 拆出 `handleAlert`：P2PQuake 与 JMA 两条链路汇到同一处，去重 / 匹配 / 静默 / 跨标签页 / 通知 / 历史六步对两者完全一致
- 静默时段的红色穿透文案补全：此前只写「EEW / 大海啸警报」，实际震度6弱以上的地震与 L4 以上的气象警报同样穿透，文档与实现现已一致
- 回归断言从 236 项扩展到 320 项：Host 轮询器 20 项（冷启动 / 去重 / 环缓冲 / 截断 / 容错 / 按需轮询）、河川区域表 10 项、zip 读取器 4 项、JMA 电文解析 15 项、气象警报匹配 9 项、Client 增量拉取 13 项、配置字段 3 项
- `samples/` 新增 5 条気象庁官方样本电文（`jma-vxww50-landslide` / `jma-vxko-flood` / `jma-vpww56-landslide` / `jma-vpww55-heavyrain` / `jma-vpww57-stormsurge`），解析断言直接跑它们

### Notes

- 新增运行期依赖：无。新增构建期脚本与样本（気象庁コンテンツは政府標準利用規約に準拠、出典明記）
- 升级后「气象灾害」默认开启，但 L4 边界已经滤掉日常注意报，不会变成噪音源
- 实测澄清：`VPWW57` 是「高潮」不是洪水（洪水只走 `VXKO`）；「大雨危険警報」「高潮危険警報」同样带 `レベル４`，因此按「严重的气象警报」这一开关语义一并纳入，而不是按灾种挑拣

## [0.2.2] - 2026-09-10

### Fixed

- **跨标签页配置同步失效**（0.2.1 拆分引入的回归）：`13-ui-settings.js` 的 `storage` 监听器直接给 `03-settings-bridge.js` 的模块私有变量 `runtimeCfg` 赋值。拆分前两者同处一个作用域所以正常，拆分后这个名字成了自由变量，打包进带 `'use strict'` 的 bundle 会抛 `ReferenceError: runtimeCfg is not defined`，于是「其它 DSH 标签页改了配置，本页立即同步」静默失效（异常被事件监听器吞掉，用户只看到不同步）。改为经 03 新增的显式入口 `reloadFromLocal()` 回读本地镜像
- **音量草稿在机器级持久化下仍会丢改动**：设置页卸载时的补写路径用的是 `saveCfg`——它只写 localStorage 镜像，不改内存也不推 Host；有 Host settings 时，下一次同步会被 Host 的旧值覆盖回来，所以「拖动音量后 300ms 内关闭设置页」照样丢改动（0.2.0 声称修过这个场景，实际只在无 Host 的回退路径下成立）。改走 `applyCfg`，内存 / 镜像 / Host 三处一致

### Changed

- `scripts/check-imports.mjs` 增加第二类检查：**赋值给本文件未声明、也未 import 的名字**。ESM 严格模式下这行必然抛 `ReferenceError`，而打包器不会报错；此前它既逃过「漏 import」检查，也逃过回归测试——0.2.1 的两处回归正属此类。当前 15 个模块 / 106 个导出名全绿
- 回归断言从 230 项扩展到 236 项：新增音量兜底落盘必须推 Host、`reloadFromLocal` 回读其它标签页写入的配置并同步内存副本
- 文档同步：README 三语修正「Host 半边仅保留插件装载所需的空壳」的过时描述（0.2.0 起 Host 已承担 settings namespace 与 `/areas` 只读路由）；DESIGN.md 修正 M1 验收处的断言数、0.2.1 之后的 Client 源码形态（15 个 ESM + rollup 打包、运行期单文件 bundle）与 `check-imports.mjs` 的职责说明

## [0.2.1] - 2026-09-10

### Changed

- **客户端源码模块化**：`client/client.js`（1,568 行）按功能拆成 `client/src/` 下 15 个**标准 ESM 模块**（显式 `import` / `export`），每个文件头部写明该文件的作用、内容与依赖；`scripts/build-client.mjs` 用 rollup 打包回 DSH 要求的单文件 bundle。常量与默认配置集中到 `01-constants.js`，不再散落各功能文件
  - 为什么仍需打包：DSH 的客户端 bundle 必须是**一个模块节点**（宿主提供已构建 bundle、扁平模块图），不支持包内多文件 import。这也是 DSH 官方插件的形态——官方包只发布 `lib/client.js` 构建产物，源码是多文件 TS
  - 构建选 rollup（纯 JS 打包器，native 绑定经 napi 在进程内加载）：不 spawn 子进程、不需要下载平台二进制，受限沙箱 / CI / Windows 杀软环境都能构建——原生 esbuild 与 esbuild-wasm 在本环境都会因 spawn 子进程被拒
  - 构建脚本把**循环依赖**升级为构建失败；新增 `scripts/check-imports.mjs` 做跨模块引用检查（用了别的模块的导出却没 import 时报错），把这类问题从运行时的 ReferenceError 提前到检查阶段
- `package.json` 脚本：`build`（打包）、`check`（引用检查 → bundle 新鲜度 → 语法）、`test`（先打包再跑回归）；devDependencies 增加 rollup

### Notes

- **DSH 0.1.5-rc.1 适配确认**：本项目用到的接口在该版本均未变更，无需改动——Host `settings.register(ns, schema)`、Host `webServer.register({kind, path, handler})`、Client `settings.section` slot（`{id, order, label}`，运行时 `quake-alert` 仍 active）、Client `ctx.settingsScope.bind({ namespace })`
- M2 剩余的泥石流 / 洪水（JMA 新数据源 + 新解析器）调整到 0.3.0

## [0.2.0] - 2026-09-10

### Added

- **静默时段**：每天固定时段内不打扰（按浏览器本地时间判定，开始晚于结束表示跨午夜，右开区间）。设置页可启用并设置起止时间；`red` 级别（EEW、大海啸警报）默认穿透、可在设置页关闭；取消 / 解除消息不穿透；静默期间命中的预警仍记入历史并标注原因
- **机器级持久化**：Host 侧用 schemastery schema 注册 settings namespace `quake-alert`，配置持久化到机器的 `settings.yaml`，Client 经 `ctx.settingsScope` 读写。scope 快照是同步可读的，所以 WebSocket 重连、`handleRaw` 等内部读取保持 M1 的同步语义；写入先更新内存与 localStorage 镜像，再异步推给 Host。首次启用时把已有的本地配置一次性迁移到 Host（之后 Host 被清空视为用户的显式操作，不再回推）；没有 settings 提供方、页面非 loopback、或 Host 只做进程内存储时自动退回 localStorage。设置页显示当前配置的存储位置
- **市区町村级匹配**：新增 `watch.cities`，可把地震情报细化到市町村。Host 侧 `/dsh-quake-alert/areas` 提供 47 都道府县 / 1,917 个市町村（総務省「都道府県コード及び市区町村コード」+ 政令指定都市の行政区，构建期合并、运行期不联网），设置页按县提供「搜索 + 多选」。匹配侧先把观测点 addr 归一到市町村全称再与选择比对，覆盖気象庁 / P2PQuake 的多种官方写法：政令市短名（大阪北区茶屋町 → 大阪市北区）、特别区带县短名（東京千代田区大手町 → 千代田区）、重名消歧（福島伊達市 → 伊達市）、北海道支庁名（渡島北斗市 → 北斗市、日高地方日高町 → 日高町）、仮名表记（龍ケ崎市 ↔ 龍ヶ崎市）。区域级数据（`isArea: true` 的区域名、EEW 区域名、海啸予報区名）与归一不到市町村的观测点（机场等）一律放行，宁可多提醒也绝不漏报
- **修复 551 的 pref 简写漏报**：实测直播数据里 `points[].pref` 出现过「京都」这类简写，与用户勾选的「京都府」永不相等 → 解析时统一归一为都道府县全称
- 运行期新增依赖 `@deepseek-ai/schemastery`（Host 侧 schema；npm 公开包，`^3.18.2`）

### Fixed

- **EEW 提醒颜色被降级**（0.1.3 引入的回归）：0.1.3 把「按命中区域强度着色」也套用到了 EEW，预测震度刚过阈值时会显示成橙色。EEW 是警报本质，恢复恒为红色；地震仍按命中区域震度着色，海啸按自身等级
- **音量草稿丢失**：拖动音量后 300ms 内关闭设置页会丢掉改动 → 卸载时补写盘；「试听」按钮改用当前草稿值而非已落盘值
- **状态指示无障碍**：侧边栏状态圆点补 `role="status"` 与 `aria-label`（此前只有颜色和 title）

### Changed

- 回归断言从 130 项扩展到 230 项：新增静默时段的时间判定（跨午夜、右开区间、非法时间回退）与穿透规则、settings 桥（迁移 / Host 优先 / 回退 / memory / 写入路径 / 异常容错）、Host schema 与 Client 默认值一致性、Host `/areas` 路由输出、addr 归一（政令市短名 / 特别区 / 消歧 / 支庁名 / 仮名表记 / 机场放行）、pref 简写归一，以及真实市区町村表结构与 10 条实测直播 addr 的归一校验

## [0.1.3] - 2026-09-10

### Added

- **取消 / 解除提醒**：此前提醒过的 EEW 被取消、或海啸预报被解除时，补一条下行音提醒（标题「紧急地震速报已取消」/「海啸预报已解除」），说明先前的警报已作废；未曾提醒过的事件只记历史、不打扰。跨标签页经 `BroadcastChannel` 同步事件键，任一标签页都能正确判断
- **侧边栏连接状态指示**：注册到 `sidebar.footer.action` 的状态圆点（绿＝已连接 / 黄＝连接中·重连中 / 红＝已停止），悬停显示详情；与设置页共用同一份状态映射
- 音效新增 `cancel`（下行音），用于取消 / 解除提醒

### Fixed

- **提醒里看不到震度**（P1）：0.1.2 删除 `second` 字段后，551 / 556 的 headline 只剩「震源 X · M」。现在 551 追加「· 最大震度3」、556 追加「· 预测最大震度5强」；无震度的震源情报不追加噪音
- **配置版本变化会清空用户配置**（P2）：`loadCfg()` 原本在 `version` 不等于当前值时直接写回默认值，一次版本号变化就会静默丢掉用户选好的关注地区与阈值。改为按当前 schema 逐字段归一、保留可识别字段后再写回当前版本号
- **历史记录字段被污染会崩设置页**（P2）：`label` / `issued` / `pref` / `suppressedReason` 若是对象或数组，会原样交给 React 抛「Objects are not valid as a React child」。现在读入与写入都经 `normalizeHistoryEntry` 规整为字符串 / 布尔，渲染层再 `String()` 兜底；缺 `key` 的旧记录补 `legacy-N` 兜底键
- **前台同时弹 toast 与系统通知**（P2）：与 DESIGN 第 7 节「可见 → toast，后台 → 系统通知」不符。现在页面可见只弹 toast，后台才发系统通知（不可用时回退 toast），也不再出现系统通知失败后重复弹第二条 toast
- **取消消息静默**（P2）：EEW 取消 / 海啸解除此前一律只记历史，用户不知道已发出的警报作废
- **`client.start()` 在 `ctx.effect` 之外**（P2）：若同一 `apply` 后续注册抛错，会留下一条没有清理器的 WebSocket 连接。改为在 effect 内 start、卸载时 stop
- **去重窗口在系统时间回拨时失效**（P3）：`seen` / `eventSeen` / `tabAlerted` 的清理条件补上 `v > now`
- **toast 颜色按全日本最大震度**（P3）：改为按命中区域的实测 / 预测震度着色，关注县震度低时不再显示成红色

### Changed

- 音量滑块改为本地草稿 + 300ms 防抖落盘，拖动时不再每移动 1px 写一次 localStorage
- 设置页监听 `storage` 事件：其它 DSH 标签页改了配置后本页立即同步
- 回归断言从 105 项扩展到 130 项：新增震度 headline、配置版本迁移、脏历史字段规整、EEW 取消 / 海啸解除、前台·后台通知分支、跨标签页取消同步、命中区域着色，并把沙箱实测样本 `ws-sandbox-20230905-fukushima.json` 纳入回归
- `scaleText()` 从死代码变为 headline 的震度来源

## [0.1.2] - 2026-09-09

### Fixed

- EEW 未携带区域数据时提示「本条为震源情报」——措辞错（EEW 不是震源情报）→ 按 `kind` 区分：EEW 提示「本条 EEW 未携带区域数据，无法按阈值判定」

### Changed

- 清理死代码与未用字段：`unique()`、`prefOfName()`（区域归一改造后已无消费者）、Alert 的 `second` 字段
- 同步 DESIGN.md：Alert 模型字段对齐实现（`issued` / `hypo` / `eventKey` / `strength`）、新增区域名归一与三层去重说明、M1 标记为已达成、修正「无需自建地理数据」的旧结论
- **测试组件纳入仓库**：`tests/`（`sync-test.cjs`、`area-tables.cjs`）与 `samples/`（5 个真实消息样本 + 说明）从 `.gitignore` 移出。入库前已扫描确认：无本地绝对路径、无凭据 / 密钥、无 `eval` / `child_process` / 网络请求、无编码 payload、无控制字符、无 BOM；唯一外部 URL 是注释中的 P2PQuake 规格文档链接。样本与区域表均为公开信息（P2PQuake 公开 API / 気象庁 公开区域名称），克隆仓库后可直接运行 105 项回归测试
- README 提供三语版本：英文为 `README.md`（主），新增 `README.zh.md`（中文）与 `README.ja.md`（日语），三份文档顶部互相链接；README 同步说明 `samples/` / `tests/` 随仓库分发、`DESIGN.md` 不上传仓库
- `package.json` 的 `files` 补上 `CHANGELOG.md` 与两份 README 译文，使其随 npm 包分发

## [0.1.1] - 2026-09-09

### Fixed

- **关注特定都道府县时海啸 / EEW 静默漏报**（P1）：区域名归一不再用「正则截取到第一个 都/道/府/県」，改为「显式区域表 + 47 县名按长度降序前缀匹配 + 556 府県予報区名兜底」三层策略。修复前受影响的 16 个都道府县会勾选了关注却收不到对应预警：
  - 「京都府」被截断为「京都」，与关注列表永不相等 → 京都府的海啸与 EEW 全部漏报
  - 66 个津波予報区中 17 个名称不含县名（東京湾内湾 / 伊豆諸島 / 小笠原諸島 / 相模湾・三浦半島 / 佐渡 / 陸奥湾 / オホーツク海沿岸 / 伊勢・三河湾 / 淡路島南部 / 隠岐 / 有明・八代海 / 壱岐・対馬 / 種子島・屋久島地方 / 奄美群島・トカラ列島 / 沖縄本島地方 / 大東島地方 / 宮古島・八重山地方）
  - EEW 的北海道区域用地方名（上川地方北部 等 36 个）、东京都用岛屿名（伊豆大島 / 新島 / 神津島 / 三宅島 / 八丈島 / 小笠原）→ 关注「北海道」「東京都」时对应 EEW 漏报
  - 跨县区域（東京湾内湾 → 千葉県/東京都/神奈川県、有明・八代海 → 福岡県/佐賀県/長崎県/熊本県）现展开为多条 region，关注任一县均可命中
  - 未能识别归属县的区域名不再静默：未命中原因中明确提示「另有 N 个区域名未能识别归属县」；全日本模式（未选地区）下仍会提醒
- **localStorage 被污染导致插件整体加载失败**（P2）：读入数据一律做类型校验，脏数据只影响对应字段，不再拖崩整个插件
  - 「最近预警」历史必须是对象数组；非数组（对象 / 数字 / 字符串 / null）或含 null 元素时规整为空数组或过滤掉非对象元素。此前 `loadJSON(HISTORY_KEY, []).slice(0, 30)` 会抛 `TypeError: loadJSON(...).slice is not a function`，使 Client 半边整体加载失败，设置页与 WebSocket 连接一起消失
  - 配置逐字段校验：`watch.prefectures` 必须是数组，且只保留 47 县中真实存在的名字并去重；布尔 / 数值字段类型不符时回退默认值，数值越界时 clamp（volume 0–1、震度 0–70、去重窗口 1–1440 分钟）；`tsunamiGrade` 改为白名单校验
  - 区域名 / 标签 / 等级等「外部数据驱动」的字典查找改用自有属性检查（`own()`）：`constructor` / `toString` 之类的键不再命中原型链（此前 `AREA_PREF['constructor']` 会返回 Object 构造函数并使 `.slice()` 抛错）
- 音量滑块：`cfg.notify.volume || 0.7` 让音量 0 被显示成 70%（拖到 0 后回弹）→ 改为直接使用已校验的数值
- 「最近预警」写盘从 20 条提升到 30 条，与内存和文档一致（此前刷新页面后记录会掉一半）
- 重连计数从「第 0 次」改为「第 1 次」；`restart()` 现在重置退避计数，切换数据源后从 1s 重新开始，不再沿用上一条连接的退避进度（否则可能等满 60s）
- 「测试系统通知」在权限被拒 / 浏览器不支持 / 未授权时给出明确文字反馈，不再点了没反应；授权成功后直接补发一条测试通知
- 震源情报不再静默：551 的「震源情报 / 远地地震」没有 `points`，此前只显示「未命中」→ 未命中原因明确说明「本条为震源情报，无震度数据，无法按阈值判定」
- `notificationSupported()` 原用 `'Notification' in window` 判断，属性存在但值不是构造函数时会误判并在发送通知时抛错 → 改为 `typeof window.Notification === 'function'`
- 删除两处死代码：`alert.kind !== 'other'`（恒为真）与 Host 半边空的 `ctx.on('dispose')` 监听
- toast 的 z-index 由 2147483000 降为 2000：高于 DSH 前端自身层级（最高约 1100），但不再用极端值压住一切

### Added

- 事件级去重：同一地震的多次发布不再连续响铃——551 用 `earthquake.time`（速报 / 震源 / 详报共享同一发生时刻），556 用 `issue.eventId`（多报 serial 递增）；强度未升级时只更新历史记录并标注「未重复提醒」，震度升级时才再次提醒
- 跨标签页去重：多开 DSH 页面时用 `BroadcastChannel` 协商，同一条预警只由一个标签页播报（不支持该 API 时退化为各自提醒，不影响正确性）

### Changed

- 回归测试从 33 项扩展到 105 项：新增気象庁 188 个 EEW 区域名与 66 个津波予報区全量归一断言、存储与原型链污染注入、事件级去重与强度升级、跨标签页去重、重连状态机等用例
- README 修正「EEW 延迟约 70ms 级」的说法（实测样本从气象厅发布到 P2PQuake 转播约 811ms，改为「数百毫秒级」），并补充智能去重、多标签页连接数、震源情报三项说明

## [0.1.0] - 2026-09-08

### Added（M1 MVP：日本地震 / 海啸实时预警）

- P2PQuake WebSocket 实时推送接收：`wss://api.p2pquake.net/v2/ws`
- 断线自动重连：指数退避（1s → 60s 封顶），应对 P2PQuake 约 10 分钟强制断线
- 解析器：code 551（地震情报：震度速报/震源/各地震度）、556（紧急地震速报 EEW，含取消）、552（海啸预报）
  - 统一预警对象（Alert）；556 的 `pref` 简写从 `areas[].name` 归一为都道府县全称
- 匹配引擎：关注都道府县多选（默认全日本）× 分级阈值（实测/预测震度、海啸等级）× 消息 id 去重
- 通知：Web Audio 合成三种灾害音色（地震/EEW/海啸）+ 音量调节；前台 toast、后台系统通知（Web Notification）
- 设置页：设置 → 灾害预警（关注地区、阈值、声音、系统通知、试听/测试按钮、数据源切换）
- 数据源切换：正式源（实时）与沙箱源（回放 2023 年历史，约 30 秒/条）
- 历史记录：最近 30 条（含未达阈值未提醒的灰色条目），点击展开明细，支持清空
- 配置持久化：localStorage（`dsh.quakeAlert.v1`），带版本字段
- 主链路接收计数与连接状态指示（绿/黄/红 + 详情）
- 解析器 / 匹配引擎对真实样本的回归测试（tests/sync-test.cjs）

### Fixed

- 设置页区块辅助函数只渲染首个子元素（多子元素被丢弃）→ 改为 rest 参数展开
- 深色主题下原生下拉框选项难以辨识 → select/option 统一白底黑字
- WebSocket 推送使用 `_id`（非 `/history` 的 `id`）导致 id 全空、历史记录互相覆盖恒为 1 条 → 解析兼容 `_id` + 匿名 key 兜底
- 配置写入经由 React updater 副作用，时机不确定 → 改为同步写盘
- 最近预警记录只展示单条、无展开反馈 → 可滚动列表 + 点击展开完整明细
